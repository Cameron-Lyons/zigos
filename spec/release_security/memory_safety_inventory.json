{
  "schema_version": 1,
  "scan_patterns": [
    "@ptrFromInt",
    "@intFromPtr",
    "@ptrCast",
    "@alignCast",
    "asm",
    "extern struct",
    "packed struct",
    "volatile",
    "allowzero",
    "@fieldParentPtr",
    "@atomic"
  ],
  "required_review_state": "release-reviewed",
  "unsafe_surface_inventory": [
    { "path": "src/check_release_security_gate.zig", "categories": ["release-gate-static-scan"], "owner": "release-security-review", "review_state": "release-reviewed", "mitigations": ["scanner constants are data-only and the tool does not dereference untrusted pointers"], "tests": ["./scripts/zig.sh build release-security-check"] },
    { "path": "tools/check_production_readiness.zig", "categories": ["release-gate-static-scan"], "owner": "release-tooling-owner", "review_state": "release-reviewed", "mitigations": ["required syscall entry symbol names are data-only scanner inputs, and the checker does not execute or dereference them"], "tests": ["./scripts/zig.sh build prod-readiness", "./scripts/zig.sh build release-security-check"] },
    { "path": "tools/generate_userspace_archive.zig", "categories": ["generated-abi-layout"], "owner": "release-tooling-owner", "review_state": "release-reviewed", "mitigations": ["generated structs are deterministic release inputs"], "tests": ["./scripts/zig.sh build userspace-images"] },
    { "path": "src/userspace/service_main.zig", "categories": ["inline-assembly"], "owner": "userspace-runtime-owner", "review_state": "release-reviewed", "mitigations": ["service entry assembly is generated by the freestanding runtime"], "tests": ["./scripts/zig.sh build userspace-images"] },
    { "path": "src/userspace/runtime.zig", "categories": ["extern-abi", "volatile-pointer"], "owner": "userspace-runtime-owner", "review_state": "release-reviewed", "mitigations": ["native syscall requests are fixed-size extern records, and the freestanding runtime reaches the assembly entry stubs through fixed extern declarations"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/interrupts/idt64.zig", "categories": ["extern-abi", "pointer-int-conversion"], "owner": "interrupt-owner", "review_state": "release-reviewed", "mitigations": ["the 16-byte gate and unaligned 10-byte IDTR layouts are compile-time asserted, and handler addresses are split into fixed-width fields before the centralized IDT load"], "tests": ["./scripts/zig.sh build kernel-x86_64-compile-check"] },
    { "path": "src/userspace/component_main.zig", "categories": ["inline-assembly"], "owner": "userspace-runtime-owner", "review_state": "release-reviewed", "mitigations": ["component entry assembly delegates to runtime start code"], "tests": ["./scripts/zig.sh build userspace-images"] },
    { "path": "src/kernel/interrupts/gdt64.zig", "categories": ["extern-abi", "pointer-int-conversion"], "owner": "interrupt-owner", "review_state": "release-reviewed", "mitigations": ["the CPU-defined 104-byte TSS and unaligned 10-byte GDTR layouts are compile-time asserted, the I/O bitmap is disabled, double faults use a dedicated aligned IST stack, and user data/code selectors are ordered for architectural SYSRET"], "tests": ["./scripts/zig.sh build kernel-x86_64-compile-check", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/interrupts/syscall64.zig", "categories": ["extern-abi", "pointer-int-conversion", "volatile-pointer"], "owner": "interrupt-owner", "review_state": "release-reviewed", "mitigations": ["the per-CPU GS state layout and STAR selector encoding are compile-time asserted, kernel stacks must be nonzero and 16-byte aligned, and initialization reads back the architectural syscall MSRs before boot continues"], "tests": ["./scripts/zig.sh build kernel-x86_64-compile-check", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/boot/common.zig", "categories": ["inline-assembly"], "owner": "boot-owner", "review_state": "release-reviewed", "mitigations": ["halt/sti loops are constrained to boot control flow"], "tests": ["./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/boot/handoff.zig", "categories": ["pointer-int-conversion"], "owner": "boot-owner", "review_state": "release-reviewed", "mitigations": ["Multiboot2 aligned tags are normalized only after magic, total-size, tag-stride, physical-extent, command-line, memory-map, and framebuffer bounds validation"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build x86_64-userspace-launch-check", "./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/utils/spin.zig", "categories": ["inline-assembly"], "owner": "arch-owner", "review_state": "release-reviewed", "mitigations": ["the shared bounded-wait hint emits only PAUSE on x86-64 and a compiler memory barrier elsewhere; it exposes no memory or port-I/O operations"], "tests": ["./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/utils/stack_watermark.zig", "categories": ["pointer-int-conversion", "inline-assembly"], "owner": "boot-owner", "review_state": "release-reviewed", "mitigations": ["boot-stack watermark reads walk only the fixed stack_bottom..stack_top linker range and report a peak; no external input reaches the pointer math"], "tests": ["./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/interrupts/isr.zig", "categories": ["pointer-cast", "inline-assembly", "extern-abi"], "owner": "interrupt-owner", "review_state": "release-reviewed", "mitigations": ["interrupt frame casts are bounded to registered handlers, every hardware or exception dispatch enters and leaves the shared interrupt-context depth tracker, the x86-64 vector offset is compile-time checked against the assembly frame contract, and the IDT exposes no user-callable syscall or yield gates"], "tests": ["./scripts/zig.sh build kernel", "./scripts/zig.sh build kernel-x86_64-compile-check", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/interrupts/context.zig", "categories": ["atomic"], "owner": "interrupt-owner", "review_state": "release-reviewed", "mitigations": ["one bounded atomic depth counter distinguishes interrupt dispatch from task and syscall context; unmatched leave operations fail closed"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build kernel"] },
    { "path": "src/arch/x86.zig", "categories": ["inline-assembly", "port-io"], "owner": "arch-owner", "review_state": "release-reviewed", "mitigations": ["all x86 port, control-register, flag, halt, model-specific-register, and control-register assembly is centralized; PCID CR3 composition rejects unaligned or out-of-range roots and identifiers, the INVPCID descriptor has a fixed two-u64 layout, and syscall enablement is read back from EFER"], "tests": ["./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-architecture-compile-check", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/arch/x86_compile_check.zig", "categories": ["packed-abi"], "owner": "arch-owner", "review_state": "release-reviewed", "mitigations": ["compile-only descriptor fixture is never linked into or executed by a boot image"], "tests": ["./scripts/zig.sh build x86_64-architecture-compile-check"] },
    { "path": "src/arch/x86_64/long_mode_probe.zig", "categories": ["inline-assembly", "port-io"], "owner": "arch-owner", "review_state": "release-reviewed", "mitigations": ["the probe uses only fixed COM1 and QEMU-exit ports after an assembly-built 128 MiB identity map, validates the Multiboot2 handoff, and never joins a production image"], "tests": ["./scripts/zig.sh build x86_64-long-mode-entry-check"] },
    { "path": "src/arch/cpu_features.zig", "categories": ["inline-assembly"], "owner": "arch-owner", "review_state": "release-reviewed", "mitigations": ["CPUID reads CPU state only; baseline-gated CR4 feature writes enable and verify PGE, and enable PCIDE only from an aligned current CR3 before verifying the result"], "tests": ["./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/utils/qemu_exit.zig", "categories": ["inline-assembly", "packed-abi"], "owner": "qemu-owner", "review_state": "release-reviewed", "mitigations": ["test-only QEMU exit path is isolated"], "tests": ["./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/kernel/utils/panic.zig", "categories": ["inline-assembly"], "owner": "kernel-runtime-owner", "review_state": "release-reviewed", "mitigations": ["panic loops disable interrupts and halt without exposing memory"], "tests": ["./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/boot/benchmark/suite.zig", "categories": ["pointer-cast", "pointer-int-conversion"], "owner": "benchmark-owner", "review_state": "release-reviewed", "mitigations": ["benchmark bridge casts are confined to benchmark fixtures, and the native syscall microfixture copies a linker-bounded code range into one owned executable user page with a separate writable user stack"], "tests": ["./scripts/zig.sh build benchmark"] },
    { "path": "src/kernel/memory/paging64.zig", "categories": ["pointer-int-conversion", "inline-assembly", "atomic"], "owner": "kernel-memory-owner", "review_state": "release-reviewed", "mitigations": ["four-level tables use one-frame 512-entry layouts, checked low-aperture physical addresses, hierarchy owner tags, full-range collision preflight, default-NX kernel identity and MMIO mappings, linker-bounded executable kernel text, and hardware NX/W^X enforcement for user mappings; supervisor-only shared kernel leaves are global while user leaves are never global, and global remaps invalidate the active translation; paging samples PCID mode once, precomputes validated CR3 switch values, reserves identifier zero for the kernel through a locked bounded allocator, preserves translations across nonredundant switches, and invalidates every context before reuse"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build kernel-x86_64-compile-check", "./scripts/zig.sh build x86_64-userspace-launch-check", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/kernel/memory/protection.zig", "categories": ["pointer-int-conversion"], "owner": "kernel-memory-owner", "review_state": "release-reviewed", "mitigations": ["page-aligned linker extents drive an RX/R-NX/RW-NX kernel section policy, every section permission is read back before readiness, the sole unmapped boot-stack guard is explicitly accounted for, and supervisor write protection is enabled through the centralized CR0 helper"], "tests": ["./scripts/zig.sh build kernel", "./scripts/zig.sh build kernel-x86_64-compile-check", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/kernel/memory/memory.zig", "categories": ["pointer-cast", "atomic", "allocator-invariant"], "owner": "kernel-memory-owner", "review_state": "release-reviewed", "mitigations": ["kernel heap headers are validated before free/realloc"], "tests": ["./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/platform/crash_record.zig", "categories": ["extern-abi", "memory-copy"], "owner": "diagnostics-owner", "review_state": "release-reviewed", "mitigations": ["crash records have checksums and redacted summaries"], "tests": ["./scripts/zig.sh build spec-tests"] },
    { "path": "src/kernel/platform/hardware_proof.zig", "categories": ["pointer-int-conversion"], "owner": "hardware-target-owner", "review_state": "release-reviewed", "mitigations": ["ACPI discovery requires the bounded Multiboot2 ACPI 2 handoff and a nonzero XSDT, maps physical tables through dedicated cache-disabled kernel windows only after paging is ready, caps every SDT at 1 MiB and the architectural physical-address width, and validates XSDT and child checksums before proof facts are emitted; telemetry observations remain typed and fail closed on missing firmware evidence or mismatched reader generations"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/platform/intel_vtd.zig", "categories": ["pointer-int-conversion", "volatile-pointer", "inline-assembly", "hardware-page-table"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["all PCI bus masters and external device interrupts are quiesced before setup; every remapping unit is capability-validated before mutation; coherent table walks and x2APIC extended interrupt mode are mandatory; one shared deny-by-default DMA root assigns NVMe, I225-LM, and xHCI distinct domain ids and second-stage trees with only page-aligned direction-scoped windows; overlapping windows and duplicate requesters are rejected; one shared all-not-present IRTE table blocks remappable and compatibility interrupts; root and interrupt table publication, register or queued invalidation, and both enable-status handshakes are ordered and bounded; mandatory primary fault records are range-checked and requester/address/direction bound; retained table and queue frames are never released after hardware may reference them"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/kernel/platform/smbios.zig", "categories": ["pointer-int-conversion"], "owner": "hardware-target-owner", "review_state": "release-reviewed", "mitigations": ["SMBIOS 3+ entry-point scanning is confined to the firmware scan window and table dereferences are length-capped before SKU matching; the 32-bit SMBIOS 2 entry-point path is absent"], "tests": ["./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel"] },
    { "path": "src/main.zig", "categories": ["pointer-int-conversion", "pointer-cast"], "owner": "boot-owner", "review_state": "release-reviewed", "mitigations": ["published kernel port and measured region are boot-scoped, the bootloader digest selects the exact target-specific entry source, and syscall dispatch consumes RDI/RSI/RDX while returning only status, byte count, and denial reason in RAX/RDX/R10"], "tests": ["./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/drivers/nvme_hw.zig", "categories": ["pointer-int-conversion", "volatile-pointer", "inline-assembly"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["controller BAR is mapped cache-disabled and only fixed register/doorbell offsets are dereferenced; four queue pages, a contiguous 64-page bounce region split into two slots, and a contiguous two-page PRP-list region are allocated up front inside the identity aperture; each slot owns one bounce and PRP page, at most two commands are outstanding, every CID retains an independent elapsed deadline, and out-of-order completions are accepted only while the CID remains active; the controller remains disabled and PCI bus mastering revoked until the production target has installed its VT-d requester allow-list and interrupt deny-list, and the same transaction composes up to two already-prepared I225-LM and xHCI domains without aliasing requester ids or pages; real-target attach submits one Identify write to a separately reserved unmapped guard page, requires a matching primary fault and unchanged canary, resets the controller before reclaiming the guard, and then performs bounded polled administration; the I/O completion queue enables single-message vector-zero interrupts and runtime activation installs an exact-requester remapped MSI route before interrupt-backed HLT waits are allowed; the wait path preserves timer deadlines, rechecks completion under disabled interrupts before sleeping, temporarily enables interrupt delivery only across HLT outside interrupt context, restores the caller's prior interrupt mask after each wake, and falls back to bounded polling for admin commands and interrupt handlers; every command checks primary fault records and a DMA fault disables MSI, the controller, and bus mastering and withdraws storage; error unwinding disables the controller and bus mastering; the destructive media roundtrip is never run on a production boot"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/kernel/drivers/intel_i225_hw.zig", "categories": ["pointer-int-conversion", "volatile-pointer", "inline-assembly", "extern-abi"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["only the exact I225-LM PCI id is accepted; BAR0 is type-checked and mapped cache-disabled; firmware must provide a valid permanent unicast MAC; interrupts remain masked; separate 64-entry TX/RX descriptor rings and independent 32-page TX/RX buffer regions are allocated below the managed aperture and exposed as four exact direction-scoped windows in an independent VT-d domain; bus mastering stays revoked until requester confinement; transmit submissions use one buffer per in-flight descriptor, reclaim completed descriptors in order, and return bounded backpressure when the ring is full without spinning; the interrupt top half only latches and masks causes, while the runtime drains at most eight frames per pass into a fixed 32-frame queue and rechecks pending work before idle; transmit and receive entry service the completion queue against synchronized monotonic ticks, and one second without oldest-descriptor progress disables both queues and bus mastering; every transmit submission and nonblocking receive poll checks link and VT-d faults, transmit copies and validates the six-byte unicast or broadcast destination before building the Ethernet envelope, receive parsing accepts only directed or broadcast local-experimental Ethernet frames, receive descriptors are recycled after completion, and a DMA fault disables both queues and bus mastering; the exported bridge validates lengths and returns explicit receive state"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/kernel/drivers/xhci_hw.zig", "categories": ["pointer-int-conversion", "volatile-pointer", "inline-assembly", "atomic"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["the probe accepts only an xHCI-class PCI function with a typed nonzero page-aligned memory BAR, rejects firmware-enabled bus mastering, and validates an xHCI 1.1+ capability block through one bounded cache-disabled sliding window; bounded ownership, reset, and slot setup precede exact contiguous allocation of a self-linking command ring, compact self-linking endpoint-zero and interrupt-IN rings plus a cache-line-separated report buffer per enabled slot, a 64-entry event ring and ERST, DCBAA, scratchpads, complete Device Contexts, one shared 64 KiB-aligned enumeration buffer, and one reusable Input Context; Supported Protocol ranges may be sparse but cannot overlap, their USB name and revision are validated, explicit PSI entries define accepted speed ids, and every serviced root port must resolve to one USB2 or USB3 assignment and packet size before reset or slot activity; publication fences precede checked DMA register programming, and six or eight page-granular direction-scoped windows form a dedicated VT-d requester domain with rings controller-readable and report buffers controller-write-only; deferred activation requires requester confinement, fault monitoring, interrupt remapping, and exact-vector MSI before enabling bus mastering, then enables the primary interrupter before Run/Stop and bounds the HCHalted transition with invariant-TSC time; vector 67 only atomically latches work and acknowledges x2APIC, while the native loop performs acquire-ordered volatile TRB reads, drains at most 64 entries per pass, follows the consumer cycle state, advances ERDP to the first unconsumed entry with EHB acknowledgement, and rechecks work before HLT; PORTSC writes preserve only architected sticky controls, acknowledge asserted changes, and never echo PED or LWS; one serialized TRB producer writes each control dword last, publishes before command or slot doorbells, and permits only one shared Input Context or enumeration-buffer user at a time; Address Device publishes only the Input Control, Slot, and endpoint-zero contexts with the validated root port, speed, packet size, and slot-private dequeue pointer, and requires an exact completion pointer and slot id before the port becomes addressed; the first device-descriptor request uses exact Setup, Data, and Status TRBs, accepts only the expected successful Status Stage Transfer Event, validates the descriptor prefix and speed-specific packet size, and serializes full-speed Evaluate Context before another transfer; a second exact request retrieves the complete descriptor, rejects invalid BCD versions, class/subclass relationships, USB generation, evaluated packet size, or zero configuration count, and retains the parsed identity only for the connected port lifetime; the first configuration is read through exact header and full-tree requests with short-packet events enabled, bounded by the aligned enumeration window, streamed through a fixed 255-byte descriptor scratch record, and rejected on invalid lengths, attributes, interface or endpoint counts, endpoint addresses, missing SuperSpeed companions, or header/tree disagreement; only a default-alternate HID boot-keyboard interface may be selected, USB2 and USB3 packet, burst, interval, and service-payload fields are range checked, an exact no-data Set Configuration completion must precede Configure Endpoint, and the Input Context grants only A0 plus the selected interrupt-IN DCI with CErr 3, DCS 1, and a dedicated ring before the port becomes configured; no interrupt TD is posted before configuration succeeds; Device Context links and clears occur only at successful allocation and disable boundaries; timer-backed one-second reset, command, and control-transfer deadlines prevent indefinite hardware ownership; unsupported speeds, DMA faults, invalid port, command, or transfer events, unsupported event types, ERDP rejection, timeout, and unexpected controller halt or error states quiesce Run/Stop and interrupt enables, disable MSI and bus mastering, clear lifecycle state, and retain VT-d confinement"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/kernel/drivers/intel_i225_tx.zig", "categories": ["extern-abi"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["the advanced transmit descriptor layout is fixed at 16 bytes with compile-time size and alignment assertions; descriptor indices are bounded to 64 entries, every descriptor owns one bounded 2 KiB buffer, completion reclamation is in order, a full ring returns backpressure without overwriting device-owned descriptors, and wrap-safe oldest-submission timestamps expose deterministic stall detection"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/drivers/intel_i225_rx.zig", "categories": ["extern-abi"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["the advanced receive descriptor layout is fixed at 16 bytes with compile-time size and alignment assertions; descriptor indices are bounded to 64 entries, buffer addresses are bounded to the 128 KiB receive region, and fixed-width completion decoding rejects missing end-of-packet state, hardware errors, zero lengths, and frames larger than one 2 KiB buffer before ownership advances"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel"] },
    { "path": "src/kernel/drivers/pci.zig", "categories": ["pointer-int-conversion", "volatile-pointer", "inline-assembly", "atomic"], "owner": "kernel-device-owner", "review_state": "release-reviewed", "mitigations": ["PCIe configuration addresses derive only from the checksum-validated segment-zero MCFG allocation and bounded bus/device/function/offset fields; one page-aligned cache-disabled kernel window is remapped under a spin lock, so no unbounded ECAM aperture or unstable mapping can be dereferenced; boot disables INTx on every discovered function and walks bounded cycle-detected standard capability chains to disable MSI and mask/disable MSI-X before VT-d activation; malformed chains and failed readback stop production boot"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check"] },
    { "path": "src/native/core/abi.zig", "categories": ["extern-abi", "packed-abi"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["ABI records are fixed-size typed syscall contracts"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/kernel/net/ethernet.zig", "categories": ["packed-abi", "pointer-cast"], "owner": "network-owner", "review_state": "release-reviewed", "mitigations": ["packet header parsing uses fixed Ethernet header layout"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/core/signing.zig", "categories": ["pointer-cast"], "owner": "signing-owner", "review_state": "release-reviewed", "mitigations": ["provider context casts are confined to provider adapters"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/core/indexed_arena.zig", "categories": ["generated-abi-layout"], "owner": "native-core-owner", "review_state": "release-reviewed", "mitigations": ["index tables use bounded fixed capacities"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/core/ids.zig", "categories": ["extern-abi"], "owner": "native-core-owner", "review_state": "release-reviewed", "mitigations": ["id wrappers preserve raw integer identity without pointer aliasing"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/platform/attestation_service.zig", "categories": ["pointer-cast"], "owner": "attestation-owner", "review_state": "release-reviewed", "mitigations": ["provider context casts are confined to attestation provider adapters"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/platform/measured_boot.zig", "categories": ["pointer-cast"], "owner": "boot-owner", "review_state": "release-reviewed", "mitigations": ["release-provider test backend casts are confined to provider adapter contexts and validate public-key identity before signing artifact manifests"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests"] },
    { "path": "src/native/platform/event_ledger.zig", "categories": ["extern-abi", "pointer-cast", "allocator-owned-buffer"], "owner": "diagnostics-owner", "review_state": "release-reviewed", "mitigations": ["persistent ledger records export redacted text; freestanding ledgers allocate their bounded event arena and indexes before the first append or persisted-state load, publish backing only after complete initialization, leave sequence state unchanged on allocation failure, and scrub and release backing during session teardown and early-ledger migration"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/platform/compositor_session.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "ux-owner", "review_state": "release-reviewed", "mitigations": ["the first freestanding window, permission-review item, and surface presentation independently allocate and explicitly initialize bounded window, review, and surface state; empty lookups remain allocation-free, allocation failure publishes no window, item, or surface, snapshot restore preallocates every required backing before replacing live state, closing the final window scrubs and releases its state, and session reset scrubs and releases all complete allocations"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/platform/input_router.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "ux-owner", "review_state": "release-reviewed", "mitigations": ["the first freestanding routed event allocates exactly one fixed-capacity event-slot array, initializes its complete reusable free list before publishing the pointer, drops the event before publishing a slot or incrementing queue counts when allocation fails, retains direct indexed access while live, and session reset scrubs and releases the complete allocation"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/platform/update_health.zig", "categories": ["extern-abi"], "owner": "update-owner", "review_state": "release-reviewed", "mitigations": ["boot witness record is bounded and checked"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/services/userspace_service_ipc.zig", "categories": ["pointer-int-conversion"], "owner": "service-ipc-owner", "review_state": "release-reviewed", "mitigations": ["IPC requests cross the typed syscall surface"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/services/package_service.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "package-owner", "review_state": "release-reviewed", "mitigations": ["the first freestanding package install allocates one fixed-capacity bundle arena after request, signature, trust, and policy validation; allocation failure is reported before reserving or publishing a bundle; all lookups tolerate the unallocated empty state; and session reset scrubs and releases the complete arena before replacing its owner"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/session/proofs/service_path_proofs_service_boundary.zig", "categories": ["pointer-int-conversion"], "owner": "session-proof-owner", "review_state": "release-reviewed", "mitigations": ["proof-only syscall calls exercise boundary checks"], "tests": ["./scripts/zig.sh build spec-tests"] },
    { "path": "src/native/services/component_abi_wire.zig", "categories": ["extern-abi"], "owner": "component-abi-owner", "review_state": "release-reviewed", "mitigations": ["component wire records are fixed-size extern request/response structs"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/task/task_runtime_model.zig", "categories": ["packed-abi"], "owner": "task-runtime-owner", "review_state": "release-reviewed", "mitigations": ["task model flags use typed packed records"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/task/task_runtime.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "task-runtime-owner", "review_state": "release-reviewed", "mitigations": ["the first freestanding task creation or non-empty snapshot restore allocates one full-capacity cold-record slab and address-space arena before publishing task or restored state; every live task binds only to bounded cold and address-space slots, allocation failure is reported before state publication, empty restore and runtime reset scrub and release both complete allocations, and hosted tests retain inline storage"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/task/address_space_retirement.zig", "categories": ["pointer-cast"], "owner": "task-runtime-owner", "review_state": "release-reviewed", "mitigations": ["type-erased retirement callback contexts are constructed through one typed adapter and invoked only through the bound runtime sink"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/task/userspace_bootstrap_mailbox.zig", "categories": ["extern-abi", "packed-abi"], "owner": "userspace-runtime-owner", "review_state": "release-reviewed", "mitigations": ["mailbox layout is fixed and measured in launch provenance"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/task/userspace_registry.zig", "categories": ["packed-abi", "bounded-static-pointer"], "owner": "userspace-runtime-owner", "review_state": "release-reviewed", "mitigations": ["runtime bundle pointers originate only from compile-time catalog strings, their lengths are compile-time checked into a six-bit field, and all consumers reconstruct bounded slices through the bundleId accessor"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build prod-readiness"] },
    { "path": "src/native/task/accelerator_scheduler.zig", "categories": ["pointer-cast"], "owner": "scheduler-owner", "review_state": "release-reviewed", "mitigations": ["provider context casts are confined to scheduler provider adapters"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/task/userspace_scheduler.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "scheduler-owner", "review_state": "release-reviewed", "mitigations": ["the first freestanding accelerator claim allocates one bounded claim arena and task index after validating the task and duplicate key, allocation failure leaves claim and queue state unchanged, all linked indices remain capacity-bounded, and scheduler reset or teardown scrubs and releases complete backing"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/task/task_runtime_host.zig", "categories": ["pointer-int-conversion"], "owner": "task-runtime-owner", "review_state": "release-reviewed", "mitigations": ["host slot index math is bounded to runtime tables"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/task/task_runtime_service.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "task-runtime-owner", "review_state": "release-reviewed", "mitigations": ["the first freestanding runtime checkpoint allocates exactly one full-capacity Snapshot, zero-initializes it, writes every published header and live record before setting the checkpoint-valid flag, reports allocation failure to callers, and session reset scrubs the complete snapshot before freeing it"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/task/userspace_executor.zig", "categories": ["pointer-int-conversion", "pointer-cast", "inline-assembly", "allocator-owned-buffer"], "owner": "task-runtime-owner", "review_state": "release-reviewed", "mitigations": ["executor maps only inspected image segments and bootstrap mailbox addresses, x86-64 yield/resume snapshots use a compile-time-checked complete general-register frame, every userspace transition returns through the typed kernel address-space switch, and the shared 48 KiB transition stack reserves an unmapped guard page while reporting a painted high-water mark; the first freestanding address-space materialization allocates one fixed-capacity mapping arena before reserving a slot, allocation failure leaves no published mapping, and executor reset destroys every nested address space before scrubbing and releasing the arena"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/session/native_store_mount.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["the freestanding session allocates one full-capacity storage checkpoint state before binding production storage and allocates bounded export-package and sync-resident backing only when verification first requests each one; every allocation is initialized before publication, allocation failure aborts the requesting boot path, and reset releases nested state before scrubbing and freeing every owning allocation"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test", "./scripts/zig.sh build storage-durability-qemu-test"] },
    { "path": "src/native/session/service_graph_builder.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "session-owner", "review_state": "release-reviewed", "mitigations": ["service-graph startup allocates exactly one full package service before constructing the environment; initialization zeros the allocation and restores the service identity plus principal-key arena and multimap sentinels before publication while leaving bundle storage deferred; allocation failure aborts startup, hot paths retain a direct service pointer, and session reset releases nested bundle ownership before scrubbing and freeing the service"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/session/supervisor.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "diagnostics-owner", "review_state": "release-reviewed", "mitigations": ["normal registration, health, contract, and driver-attachment state remains authoritative in live records without retained duplicates; the first freestanding crash or restart transition allocates one fixed 2048-byte ring, zeros it before publication, and drops only the diagnostic when allocation fails while preserving the service transition; every ring access is cursor-bounded, and session reset scrubs and frees the complete allocation"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/session/session_manager_contexts.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "session-owner", "review_state": "release-reviewed", "mitigations": ["service-graph startup allocates exactly one full-capacity capability table, userspace catalog, userspace scheduler, and task runtime before environment or bootstrap-state construction; initialization zeros each allocation and restores capability indexes and generation state, userspace image arena and id state, scheduler arena and queue state, and task arena, owner index, label index, id sequence, and lifecycle generation before publication; allocation failure aborts startup, hot paths retain direct pointers, and session reset deinitializes nested scheduler and runtime ownership before scrubbing and releasing all four allocations"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/session/proofs/service_path_proofs_common.zig", "categories": ["pointer-int-conversion"], "owner": "session-proof-owner", "review_state": "release-reviewed", "mitigations": ["proof-only syscall calls use typed request/response structs"], "tests": ["./scripts/zig.sh build spec-tests"] },
    { "path": "src/native/session/session_manager_boot_flow.zig", "categories": ["pointer-cast"], "owner": "session-owner", "review_state": "release-reviewed", "mitigations": ["the focused-input callback context is bound only from the address of the manager-owned router, is cast back only in that typed adapter, is cleared before session reset or boot failure, and the kernel rejects events whose task id does not match the authorized receiver"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/session/proofs/service_path_proofs_sync.zig", "categories": ["pointer-cast"], "owner": "session-proof-owner", "review_state": "release-reviewed", "mitigations": ["booted sync proof attestation callback casts are confined to external-provider fixture adapters and the proof requires request-bound verifier metadata digests before creating pinned network identity policy"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests"] },
    { "path": "src/native/session/proofs/service_path_proofs_syscalls.zig", "categories": ["pointer-int-conversion"], "owner": "session-proof-owner", "review_state": "release-reviewed", "mitigations": ["negative syscall proofs exercise scoped user-memory validation"], "tests": ["./scripts/zig.sh build spec-tests"] },
    { "path": "src/native/session/trust_boot.zig", "categories": ["pointer-cast"], "owner": "boot-owner", "review_state": "release-reviewed", "mitigations": ["smoke artifact-manifest signing uses a bounded release-provider adapter context and validates the key handle public key before signing"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/kernel_api/syscall_surface.zig", "categories": ["pointer-int-conversion"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["syscall dispatch tests cover invalid userspace pointers and buffers"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/kernel_api/endpoint_syscalls.zig", "categories": ["pointer-int-conversion"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["endpoint send borrows a read-validated bounded payload only until the synchronous queue copy returns; endpoint receive validates fixed-response capacity plus bounded payload and capability output ranges for overflow, alignment, mapped write access, and maximum payload length before dequeue; undersized destinations leave the message queued"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build release-security-check"] },
    { "path": "src/native/kernel_api/syscall_dispatch.zig", "categories": ["pointer-int-conversion"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["read/write helpers validate user ranges, overflow, and alignment before dereference; immediate user-slice borrows preserve identity only for synchronous consumers that copy before returning to userspace"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/kernel_api/operation_metadata.zig", "categories": ["packed-abi"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["operation scope rules are typed metadata"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/kernel_api/shared_memory.zig", "categories": ["packed-abi", "pointer-cast", "allocator-owned-buffer"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["shared-memory access flags are typed and scoped; the first freestanding object creation allocates and initializes one bounded table backing before reserving an object handle, empty queries remain allocation-free, allocation failure publishes no state, and session reset scrubs and releases the complete backing"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/kernel_api/task_syscalls.zig", "categories": ["pointer-int-conversion"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["image pointer copies route through validated user-memory helpers"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/kernel_api/endpoint.zig", "categories": ["packed-abi", "pointer-cast", "allocator-owned-buffer"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["endpoint flags are typed packed records; the first send to each freestanding endpoint allocates one fixed eight-message queue after peer and capacity validation, allocation failure leaves queue depth unchanged, all accesses are bounded by the compact queue indices, and task retirement, table reset, and owning transport or session teardown scrub and release every queue"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build x86_64-userspace-launch-check", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/kernel_api/capability.zig", "categories": ["packed-abi"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["capability rights project through typed bit records"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/kernel_api/capability/rights.zig", "categories": ["packed-abi"], "owner": "kernel-api-owner", "review_state": "release-reviewed", "mitigations": ["capability rights project through typed bit records"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/storage/storage_service_ipc.zig", "categories": ["pointer-int-conversion"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["storage IPC uses typed request/response structs through validated syscall calls"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/storage/storage_volume.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["the freestanding durable-volume log workspace is one fixed-size kernel-heap allocation created before the first backend read or save, every slice remains bounded by the durable data-region capacity, allocation failure is reported through the storage error path, and volume reset scrubs the complete workspace before freeing it"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test", "./scripts/zig.sh build storage-durability-qemu-test"] },
    { "path": "src/native/storage/object_store/store.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["freestanding chunk pointers originate only from bounded kernel-heap allocations sized to validated immutable payloads of at most 4096 bytes; live backing is never copied, length bounds precede slicing, and reset zeroes each allocation before freeing it"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test", "./scripts/zig.sh build storage-durability-qemu-test"] },
    { "path": "src/native/storage/workspace/directory.zig", "categories": ["pointer-cast", "allocator-owned-buffer"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["each live freestanding workspace exclusively owns one fixed-capacity mutation-log allocation; create and replay establish backing before access, every index remains bounded by the 192-entry product limit, allocation rollback removes the reserved slot, and directory reset zeroes the full backing before freeing it"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests", "./scripts/zig.sh build kernel", "./scripts/zig.sh build zigos-native-smoke-test", "./scripts/zig.sh build storage-durability-qemu-test"] },
    { "path": "src/native/storage/storage_service/service.zig", "categories": ["pointer-cast"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["storage callback contexts are cast only in service adapter shims"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/storage/file_bridge.zig", "categories": ["pointer-cast"], "owner": "storage-owner", "review_state": "release-reviewed", "mitigations": ["file bridge callback contexts are confined to bridge adapters"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/services/public_store.zig", "categories": ["release-gate-static-scan"], "owner": "package-owner", "review_state": "release-reviewed", "mitigations": ["static scan hit is WASM asset metadata in release fixtures; the store channel stores bounded slices, validates manifest/catalog agreement, and never dereferences asset payload bytes"], "tests": ["./scripts/zig.sh build host-tests"] },
    { "path": "src/native/drivers/network_driver_task.zig", "categories": ["pointer-cast"], "owner": "network-owner", "review_state": "release-reviewed", "mitigations": ["test external-attestation provider callback casts are confined to local fixture adapters; production service-identity connections require request-bound verifier metadata digests and broker rechecks preserve the verified evidence; a fixed-capacity peer-link directory accepts only stable one-to-one device-to-unicast-MAC bindings, includes the target MAC in session key derivation, and reserves broadcast transmit for scoped discovery; deferred receive service has a fixed poll budget and 32-frame queue, validates every device result before advancing ownership, drops overflow while continuing hardware progress, and wakes only the recorded network-task owner"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build release-security-check"] },
    { "path": "src/native/sync/sync_transport.zig", "categories": ["pointer-cast"], "owner": "sync-owner", "review_state": "release-reviewed", "mitigations": ["test external-attestation provider callback casts are confined to local fixture adapters; production-attested sync sessions pin request-bound verifier metadata digests before endpoint delivery or active-driver transmit; active-driver delivery resolves the target device through the bounded peer-link directory and reports a failed network delivery without transmitting when no route is bound"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build release-security-check"] },
    { "path": "src/native/sync/sync_transport_harness.zig", "categories": ["pointer-cast"], "owner": "sync-owner", "review_state": "release-reviewed", "mitigations": ["freestanding relay queue backing comes from aligned kernel allocation; explicit initialization restores packet-arena and session-index sentinels; packet encryption and payload bounds are validated before allocation; teardown scrubs and frees the bounded queue"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build prod-readiness", "./scripts/zig.sh build zigos-native-smoke-test"] },
    { "path": "src/native/platform/platform_policy_signals.zig", "categories": ["pointer-cast"], "owner": "scheduler-owner", "review_state": "release-reviewed", "mitigations": ["first-target reader and driver callback provider context casts are confined to telemetry provider adapters and fail closed on missing callbacks, test-only providers, target mismatches, stale generations, and incomplete typed reader proofs"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build prod-readiness"] },
    { "path": "src/tests/spec/platform_services.zig", "categories": ["pointer-cast"], "owner": "spec-platform-owner", "review_state": "release-reviewed", "mitigations": ["platform-service attestation callback casts are confined to external-provider fixture adapters and the spec requires verifier metadata digest binding before accepting remote attestation"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build spec-tests"] },
    { "path": "src/native/session/booted_evidence.zig", "categories": ["release-gate-static-scan"], "owner": "session-owner", "review_state": "release-reviewed", "mitigations": ["static scan hit is WASM asset metadata in the boot evidence fixture; install/update still resolve through the package service and public-store metadata validation before task launch"], "tests": ["./scripts/zig.sh build host-tests", "./scripts/zig.sh build zigos-native-smoke-test"] }
  ]
}
