const impl = @import("proofs/service_path_proofs_platform.zig");

pub const proveBootedPostActivationHealthChecks = impl.proveBootedPostActivationHealthChecks;
pub const proveBootedSchedulerTelemetryProvider = impl.proveBootedSchedulerTelemetryProvider;
